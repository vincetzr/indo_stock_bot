# The full runtime, not a subset.
#
# THIS FILE LISTED FOUR PACKAGES AND THAT COST A FULL REBUILD.
# 2026-09-06: the container was rebuilt cold and every pip-installed package
# vanished -- pandas, numpy, pyarrow, scikit-learn, matplotlib, pytest, the lot.
# Earlier probing had measured installs persisting across WARM RESUMES and
# explicitly flagged "does this survive a COLD build?" as unknown. It does not.
# A requirements file that is a subset of what the code imports is not a
# manifest, it is a decoration.
pandas>=2.0
numpy>=1.24
requests>=2.28
PyYAML>=6.0
pyarrow>=14          # every panel on disk is parquet; without this nothing loads
scipy>=1.11
scikit-learn>=1.3    # H27's cross-sectional model
matplotlib>=3.7      # scripts/paint_suite.py and every chart
beautifulsoup4>=4.12
lxml>=5.0
yfinance>=0.2
pytest>=7.0          # 2,393 tests
statsmodels>=0.14    # HAC/Newey-West standard errors
duckdb>=1.0          # 13x pandas on point queries over the existing parquet
tabulate>=0.9
